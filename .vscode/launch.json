{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run 2048",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${config:godotTools.editorPath.godot4}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
        }
    ]
}
